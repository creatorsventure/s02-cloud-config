# Redis Props
spring:
  cache:
    type: redis
    redis:
      time-to-live: 60000
  data:
    redis:
      host: localhost
      port: 6379
      password: redis123
      timeout: 60000
#Eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost

# security Configurations
# Keystore
security:
  keystorePath: classpath:cv_keystore.p12
  keystorePassword: Cv@123
  # Current key alias + password
  keystoreCurrentAlias:
    name: cv_1_02042025_01042030
    keyPassword: Cv@123
  # Old (expired) keys
  keystoreOldAliases:
    - name: cv_02042025_01042030
      keyPassword: Cv@123
  # JWT
  JWTSecret: oVT3Hf4kzYuzrJBc1IZxNwD1f611IlWDBlU1QKF/H2s=
  # 15 minutes
  JWTAccessTokenExpirationMins: 15
  # 7 days
  JWTRefreshTokenExpirationMins: 10080
